/**
 * Feature-level re-export.
 * The implementation lives in @/hooks/useContactForm (shared).
 * Features import from here to keep the feature boundary explicit.
 */
export { useContactForm } from '@/shared/hooks/useContactForm'
